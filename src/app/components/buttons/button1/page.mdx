import { IconButton } from "@/components/zui/buttons/icon-button";
import { FaPlus } from "react-icons/fa";

# Icon Button

This is an icon button component.

- [ ] Add a link to the button component

1. Click the button
2. See the button

```shell
# Install the button component
npm install @components/button/btn
```

<IconButton icon={<FaPlus />} iconPosition="right" hoverEffect="scale">
  Button 1
</IconButton>
